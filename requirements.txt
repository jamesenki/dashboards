# Core dependencies for Secure Model Loader functionality
mlflow==2.16.2  # Updated from 2.8.0 to fix security vulnerabilities (CVE-2024-27134, CVE-2024-2928, etc.)
pyarrow==14.0.1  # Updated from 13.0.0 to fix deserialization vulnerability (CVE-2023-47248)

# Web API dependencies
fastapi==0.115.12
uvicorn[standard]==0.27.0
pydantic==2.11.0
pydantic-settings==2.1.0

# Database access
sqlalchemy==2.0.40
redis==5.0.1
asyncpg==0.30.0
aiosqlite==0.21.0
greenlet==3.1.1

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0
httpx==0.28.1

# NOTE: The following packages have complex dependencies that may conflict
# and should be installed separately if needed:
# - scikit-learn
# - torch/torchvision
# - prophet, kats, tsfel (time series analysis)
# - numpy (version depends on other packages)
# - pandas (version depends on other packages)
