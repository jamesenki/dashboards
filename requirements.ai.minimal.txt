# IoTSphere AI Infrastructure - Minimal Requirements
# Optimized for Mac M1/M2 development environments

# Core dependencies
pydantic>=2.0.0
huggingface-hub>=0.19.0
sentence-transformers>=2.2.0

# Vector Database (simpler alternative to avoid compilation issues)
chromadb>=0.4.18

# RAG Components (using compatible versions)
langchain>=0.2.0
langchain-community>=0.1.0

# For testing
pytest>=7.0.0
